console.log(`Vaše jméno je ${jmeno}, je vám ${vek} let`)
if (vek > podminka)
  {console.log(' a tak máte právo řídit auto ')} 
    else {consol.log(`a tak musíte počkat ještě ${podminka-vekOdpoved} roků, než budete moci řídit auto')}

